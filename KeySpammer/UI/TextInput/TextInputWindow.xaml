<Window x:Class="KeySpammer.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Text"
        Width="520"
        Height="340"
        MinWidth="520"
        MinHeight="340"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <SolidColorBrush x:Key="DialogBg" Color="#10151D" />
        <SolidColorBrush x:Key="DialogSurface" Color="#151C27" />
        <SolidColorBrush x:Key="DialogCard" Color="#182131" />
        <SolidColorBrush x:Key="DialogBorder" Color="#2A3547" />
        <SolidColorBrush x:Key="DialogBorderBright" Color="#3B4A62" />
        <SolidColorBrush x:Key="DialogText" Color="#EEF4FA" />
        <SolidColorBrush x:Key="DialogMuted" Color="#8EA0B6" />
        <SolidColorBrush x:Key="DialogCyan" Color="#38BDF8" />
        <SolidColorBrush x:Key="DialogPurple" Color="#A78BFA" />

        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Height" Value="32" />
            <Setter Property="MinWidth" Value="86" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Foreground" Value="{StaticResource DialogText}" />
            <Setter Property="Background" Value="#1A2433" />
            <Setter Property="BorderBrush" Value="#354258" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid RenderTransformOrigin="0.5,0.5">
                            <Border x:Name="Root"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8" />

                            <Border x:Name="HoverOverlay"
                                    Background="#FFFFFF"
                                    CornerRadius="8"
                                    Opacity="0"
                                    IsHitTestVisible="False" />

                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HoverOverlay" Property="Opacity" Value="0.08" />
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="HoverOverlay" Property="Opacity" Value="0.14" />
                                <Setter TargetName="Root" Property="RenderTransformOrigin" Value="0.5,0.5" />
                                <Setter TargetName="Root" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogPrimaryButton"
               TargetType="Button"
               BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="#2B1664" />
            <Setter Property="BorderBrush" Value="#6D5BD0" />
            <Setter Property="Foreground" Value="#EDE9FE" />
        </Style>

        <Style x:Key="DialogCloseButton" TargetType="Button">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="24" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource DialogMuted}" />
            <Setter Property="FontFamily" Value="Segoe UI Symbol" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="False" />
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#7F1D1D" />
                                <Setter Property="Foreground" Value="#FEE2E2" />
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#991B1B" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="10"
            CornerRadius="16"
            Background="{StaticResource DialogBg}"
            BorderBrush="{StaticResource DialogBorder}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="22"
                              ShadowDepth="0"
                              Opacity="0.65"
                              Color="#000000" />
        </Border.Effect>

        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <StackPanel Margin="0,0,42,16">
                    <TextBlock Text="Add Text Step"
                               Foreground="{StaticResource DialogText}"
                               FontSize="15"
                               FontWeight="Bold"
                               FontFamily="Segoe UI" />

                    <TextBlock Text="Text will be typed into the selected target window."
                               Foreground="{StaticResource DialogMuted}"
                               FontSize="11"
                               FontFamily="Segoe UI"
                               Margin="0,3,0,0" />
                </StackPanel>

                <Button Content="×"
                        Style="{StaticResource DialogCloseButton}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Click="CancelButton_Click" />
            </Grid>

            <!-- Text input -->
            <Border Grid.Row="1"
                    CornerRadius="10"
                    Background="#0F1724"
                    BorderBrush="{StaticResource DialogBorderBright}"
                    BorderThickness="1">
                <Grid>
                    <TextBox x:Name="InputTextBox"
                             MinHeight="180"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             AcceptsTab="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled"
                             Background="Transparent"
                             Foreground="{StaticResource DialogText}"
                             BorderBrush="Transparent"
                             BorderThickness="0"
                             Padding="12"
                             FontFamily="Segoe UI"
                             FontSize="13"
                             CaretBrush="{StaticResource DialogCyan}"
                             VerticalContentAlignment="Top" />
                </Grid>
            </Border>

            <!-- Buttons -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,14,0,0">
                <Button Content="Cancel"
                        Style="{StaticResource DialogButton}"
                        Margin="0,0,8,0"
                        Click="CancelButton_Click" />

                <Button Content="Add"
                        Style="{StaticResource DialogPrimaryButton}"
                        Click="AddButton_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
