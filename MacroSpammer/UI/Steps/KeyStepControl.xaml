<UserControl x:Class="MacroSpammer.UI.Steps.KeyStepControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="False">
    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="UpArrow"
                   Grid.Row="0"
                   Text="▲"
                   FontSize="18"
                   FontWeight="Bold"
                   FontFamily="Segoe UI"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Opacity="0.95"
                   Visibility="Collapsed" />

        <Border x:Name="KeyBorder"
                Grid.Row="1"
                Height="52"
                CornerRadius="10">
            <TextBlock x:Name="KeyTextBlock"
                       FontWeight="Bold"
                       FontFamily="Segoe UI"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="NoWrap"
                       TextTrimming="None" />
        </Border>

        <TextBlock x:Name="DownArrow"
                   Grid.Row="2"
                   Text="▼"
                   FontSize="18"
                   FontWeight="Bold"
                   FontFamily="Segoe UI"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Opacity="0.95"
                   Visibility="Collapsed" />
    </Grid>
</UserControl>
