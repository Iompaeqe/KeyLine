<!--
    Inspector-owned CheckBox style. The indeterminate (dot) state is the shared mixed-value indicator for
    batch checkbox fields. Kept identical to the previous app-wide style so visuals do not change.
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource Text}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="Box" Width="14" Height="14" CornerRadius="4"
                                Background="#111827" BorderBrush="#3A4658" BorderThickness="1"
                                Margin="0,0,6,0">
                            <Grid>
                                <Path x:Name="Tick" Visibility="Collapsed"
                                      Stroke="#38BDF8" StrokeThickness="2"
                                      Data="M 2 7 L 5.5 10.5 L 12 4" />
                                <!-- Mixed / indeterminate state: an obvious filled dot inside the box. -->
                                <Ellipse x:Name="Dot" Visibility="Collapsed"
                                         Width="8" Height="8" Fill="#38BDF8"
                                         HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ContentPresenter VerticalAlignment="Center" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="#162235" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="#38BDF8" />
                            <Setter TargetName="Tick" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="Box" Property="Background" Value="#162235" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="#38BDF8" />
                            <Setter TargetName="Dot" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
